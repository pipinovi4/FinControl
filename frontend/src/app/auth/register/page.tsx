import Sidebar from "@/components/layout/Sidebar";

const RegisterPage = () => {
    return (
        <div className="h-screen">
            <Sidebar/>
        </div>
    );
};

export default RegisterPage;